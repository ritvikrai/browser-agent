import './globals.css'
export const metadata = { title: 'Browser Agent', description: 'Natural language browser automation' }
export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>
}
